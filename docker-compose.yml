services:
  seeder:
    build: .
    env_file: .env
    volumes:
      - ./output:/app/output
    # Default command shows help; override with `docker compose run seeder <command>`
    command: ["--help"]
